spring:
  application:
    name: spendless-auth-service
  main:
    web-application-type: servlet
  mvc:
    throw-exception-if-no-handler-found: true
    web:
      resources:
        add-mapping: false
  security:
    user:
      name: user@123
      password: user
  datasource:
#    url: jdbc:postgresql://localhost:5432/spendless-auth
#    username: postgres
#    password: Souvikp683@
    url: jdbc:postgresql://ep-wandering-mode-a1125o7i-pooler.ap-southeast-1.aws.neon.tech/SpendlessAuth?user=neondb_owner&password=npg_nDp91cxGVerA&sslmode=require&channelBinding=require
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  output:
    ansi:
      enabled: ALWAYS

server:
  port: 8006
  address: 0.0.0.0

springdoc:
  swagger-ui:
    path: /docs

logbook:
  format:
    style: http
  include:
    - request
    - response

logging:
  level:
    root: INFO
    org.zalando.logbook: TRACE
